/* Crear una variable de tipo string con al menos
10 caracteres y generar un nuevo string con los últimos
3 caracteres guardando el resultado en una nueva variable
(utilizar substring).*/

stringA = "morethantencharacters";
stringB = stringA.substring(stringA.length-3,stringA.length);